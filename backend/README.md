# backend/

Aquí vivirá el proyecto Spring Boot (API REST) de DuelVault.

Pendiente de generar con Spring Initializr (Maven, Java, arquitectura hexagonal). Cuando exista, las migraciones de
`db/migrations/` pasarán a
`backend/src/main/resources/db/migration/` siguiendo la convención de Flyway.
